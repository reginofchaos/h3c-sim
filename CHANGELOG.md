# 更新日志 · H3C 网络仿真实验室

> 迭代版本号自 **1.0.0** 起，按日汇总版本跨度；回滚的修改不记录。

## [1.8.0] — 2026-09-15 · 新增「STP 环路与端口阻塞实验」（三台交换机三角形环路）
- **新增实验场景「STP 环路与端口阻塞」**：排在「VLAN 划分与 trunk 实验」之后（现索引 1）。`SW1 / SW2 / SW3` 三台交换机**两两互联形成三角形物理环路**，交换机互联口为 trunk、PC 接入口为 access 加入 VLAN 10
- **端口角色一目了然**：优先级最小的 `SW1`（`stp priority 4096`）成为根桥；`SW3` 连接 `SW2` 的 `GE1/0/2` 被判为 **ALTE / DISCARDING**（拓扑上橙色指示灯）；`SW1` 两口为指定端口，`SW2`、`SW3` 各有一个根端口
- **两个可动手验证的现象**：① 在 `SW3` 上执行 `stp root primary`，根桥与阻塞端口会**一起迁移**到另一条链路；② `shutdown` 主用链路后，原本阻塞的端口自动转为转发，`PC1` 与 `PC2` 仍然互通
- **修复 `undo stp priority` 未真正恢复**：此前只把 `cfg.stp.priority` 改回 32768，漏了 `instances[0].priority`，而 `bridgeId()` 读的是后者——执行 `undo` 后设备仍会被当成根桥
- **命令引擎增强**：`undo` 命令现在允许省略尾部变量参数（真机上 `undo stp priority` 这类写法不带参数也能识别）。仅在严格匹配全部失败时才兜底，不影响既有命令与歧义判定
- 回归测试 `verify.js` 达 **394** 项断言全通过（新增 21 项 STP 环路与端口阻塞断言）

## [1.7.9] — 2026-09-15 · 新增「VLAN 跨交换机跨网段访问实验」（核心三层 + 接入二层）
- **新增实验场景「VLAN 跨交换机跨网段访问」**：三层核心 `CORE`(S5560X) 下挂 `ACC1`、`ACC2` 两台二层接入交换机，CORE↔ACC 用 trunk 承载 VLAN 10/20，网关统一放在 CORE 的 `Vlan-interface10/20` 上——还原园区网「核心三层 + 接入二层」的典型两级组网
- **场景顺序调整**：「VLAN 跨网段访问（三层交换）实验」前移到「VLAN 划分与 trunk 实验」之后，新场景紧随其后，形成 `trunk → 三层交换网关 → 跨交换机三层转发` 的递进链路（其余场景顺延）
- **转发引擎修复（重要）**：三层路径现在可以**穿越纯二层交换机**。此前 `resolveNextHop` 只认直连邻居，`PC → 接入交换机 → 三层核心` 这种组网会把下一跳停在无 IP 的接入交换机上并报 `no route`；新增 `walkToTarget()` 沿二层域下行找到网关的真正所有者
- **生成树修复（重要）**：`computeStp()` 的 Dijkstra 把根端口记成了**上游设备**的端口名（`prevPort[v] = up`，本应为 `vp`），导致核心交换机上连接两台接入交换机的 trunk 口之一被误判为 `ALTE/discarding`，上行链路被无故阻塞
- **内置对照组**：`PC1(VLAN10/ACC1)` ping `PC3(VLAN10/ACC2)` 走二层 trunk 直达、tracert 只有一跳且不出现网关；与 `PC1` ping `PC2(VLAN20/ACC2)` 的「首跳网关 + 第二跳目的」形成鲜明对比
- 回归测试 `verify.js` 达 **373** 项断言全通过（新增 16 项跨交换机跨网段断言）

## [1.7.8] — 2026-09-14 · 新增「VLAN 跨网段访问（三层交换）实验」场景
- **新增实验场景「VLAN 跨网段访问（三层交换）」**：三层交换机 CORE 上创建 `Vlan-interface10`(192.168.10.1) 与 `Vlan-interface20`(192.168.20.1) 作为两个 VLAN 的网关，PC1(VLAN10) 与 PC2(VLAN20) 由此实现跨网段互访，讲清「二层隔离、三层转发」的核心原理
- **内置对照组**：PC2 与 PC3 同属 VLAN 20，同一 VLAN 内两台主机二层直达、不经网关，与 PC1→PC2 的跨 VLAN 路径形成鲜明对比
- **可验证的关键现象**：`tracert 192.168.20.10` 首跳为网关 192.168.10.1；CORE 的 ARP 表同时学到 VLAN10 与 VLAN20 两侧主机；删掉 `Vlan-interface20` 的地址后 PC1 立即失去跨网段出口
- 回归测试 `verify.js` 达 **357** 项断言全通过（新增 17 项跨 VLAN 转发断言）

## [1.7.7] — 2026-09-12 · 选中高亮增强 + 设备库悬浮窗补充简介与使用场景
- **选中高亮显著增强**：选中设备的描边由 2.5px 细线加粗至 3.5px，填充提亮并叠加青色发光（drop-shadow），在深色画布上一眼可辨（此前几乎看不出选中态）
- **设备库悬浮窗补充内容**：移到调色板任一设备上，除原有类型/型号/规格外，新增「设备简介」与「常用使用场景」两段，帮助学生快速了解每台设备的定位与典型实验用法
- 回归测试 `verify.js` 达 **340** 项断言全通过（新增 2 项设备库悬浮窗简介/场景断言）

## [1.7.6] — 2026-09-11 · 悬浮交互增强（PC 显示 MAC / 点击空白取消连线 / 设备库悬浮详情）
- **PC / 服务器悬浮窗口新增「MAC 地址」**：鼠标移到 PC / 服务器上，悬浮小窗直接显示本机 MAC（`xxxx-xxxx-xxxx`），与状态面板、`ipconfig /all`、交换机 MAC 表 / ARP 表同源（`Sim.bridgeMac`），做 MAC 地址表实验时不必再敲命令
- **端口连线时点击空白区域取消连线**：连线过程中若另一端点点到画布空白，自动清除跟随虚线与提示横幅、取消本次连线（与 Esc 取消行为一致），避免误点后连线状态卡住
- **设备库悬浮详情**：左侧调色板的设备项新增鼠标悬浮详情——移到任一设备上即显示其类型、型号、详细介绍与端口规格（如 `1×GE + 4×FE`），便于学生认设备
- 回归测试 `verify.js` 达 **338** 项断言全通过

## [1.7.5] — 2026-09-11 · PC 状态面板显示 MAC 地址（与转发实际 MAC 一致）
- **PC / 服务器状态面板新增「MAC 地址」**：选中终端后，右侧「终端配置」面板直接显示本机 MAC（`xxxx-xxxx-xxxx`），做 MAC 地址表实验时不必再敲 `ipconfig /all`
- **统一 MAC 来源为 `Sim.bridgeMac`**：此前 `ipconfig /all` 用 `U.genMac(dev.id)`、转发实际用 `U.genMac(dev.id + dev.name)`，**两者对不上**（面板看到的 MAC 与交换机 MAC 表里的不是同一个）。现在面板、`ipconfig /all`、交换机 MAC 表、ARP 表全部一致
- `ipconfig /all` 的物理地址改为 `xxxx-xxxx-xxxx` 横杠格式（原为冒号格式），符合 Windows 显示习惯
- 回归测试 `verify.js` 达 **332** 项断言全通过

## [1.7.4] — 2026-09-11 · 补齐 display vlan brief，并修正 MAC/ARP 表学习
- **新增 `display vlan brief`（可缩写 `dis vlan bri`）**：以「VLAN ID / 名称 / 成员端口」三列简要列出全部 VLAN。此前该命令在真机上很常用，但仿真器中缺失，执行会报 Unrecognized command
- **修正 MAC 地址表学习（端口归属）**：源 MAC 原本被错记到**出端口**，现改为记在帧的**入端口**；同时补全回程方向的目的 MAC。现在交换机能学全通信双方的 MAC，且端口归属与真机一致
- **修正 ARP 表适用范围**：ARP 属三层功能，**纯二层交换机不再产生 ARP 表项**；仅路由器、PC 以及配置了三层接口（如 VLANIF）的交换机才维护 ARP 表
- **修复三层交换机 VLANIF 恒为 down**：内部接口名 `VLAN<n>` 未被 `l3IfaceInfo()` 识别为三层 VLAN 口，被误判成物理口，导致 VLANIF 永远 down、网关不通、无法演示三层转发与 ARP
- 回归测试 `verify.js` 达 **326** 项断言全通过

## [1.7.3] — 2026-09-11 · 拖拽添加设备落在鼠标指示位置（与点击竖列区分）
- **拖拽添加 vs 点击添加区分**：点击调色板仍走「左对齐竖列」自动排布（从整体左上角空白起、等间距一列）；而从调色板**拖拽**到画布松手时，设备直接**落在鼠标指示的位置**（拖放点居中），不再被重排到竖列锚点
- `addDeviceAt` 增加 `useColumn` 参数：拖拽路径传 `false` 跳过 `placeNewDevice` 列重排，仅点击路径默认 `true` 走竖列
- 列游标 `colSlot` 仅服务于点击竖列；拖拽手动落点不影响列游标逻辑
- 回归测试 `verify.js` 达 **309** 项断言全通过

## [1.7.2] — 2026-09-11 · 新建设备改为左对齐竖列排布（从整体左上角空白起）
- **新建设备放置策略重构**：从「当前拓扑整体」**左上角空白**起，左对齐、自上而下、**等间距排成一列**（行高 = 设备高 + 36px 间距），不再随机散布在图中空白位置
- **连续添加左对齐竖列**：连续点击设备库添加多台设备时，沿同一列依次下移、左对齐且行间距完全一致；首台落在整体左上角空白，后续紧贴上一台下方
- **列游标自动归零**：`colSlot` 在拓扑结构变动（清空 / 加载场景 / 删除设备 / 手动拖拽设备结束）时归零，下一轮新建重新从整体左上角空白排起
- 保留**左上角空白列优先**（整体左侧一列），左侧贴边时再落到整体正下方一列，确保新列与已有拓扑留有 30px 间距且不重叠
- 回归测试 `verify.js` 达 **299** 项断言全通过

## [1.7.1] — 2026-09-10 · 新设备自动排布到空白区域，不再叠在一起
- **新增设备自动避让**：以目标位置为起点做**网格螺旋外扩**搜索，找到第一个不与任何已有设备重叠的位置；最小留白 30×36px，并把端口指示灯行计入设备高度，避免视觉与操作上的重叠
- **连续点击设备库**添加多台时自动依次错开排布；**拖拽放置**在落到已有设备上时同样自动让开到最近的空白位
- 新设备若被排到可视区之外，自动**平移画布**使其可见（不改变缩放）
- `clientToContent` 坐标换算增加无 CTM 时的兜底（画布矩形 + 视图变换），极端环境下拖拽添加仍可用
- 回归测试 `verify.js` 达 **298** 项断言全通过

## [1.7.0] — 2026-09-10 · 拓扑交互增强：数量徽标 / 平行连线 / 虚线跟随 / 悬浮小窗
- **已连线数量改为未读消息样式徽标**：设备右上角改为圆角胶囊徽标（品牌蓝 `#2563eb` 底 + 白字，**不用红色**）；未连线的设备不再显示；设备宽度预留徽标位置，长主机名不再与徽标重叠
- **同一对设备的多条连线自动平行错开**：按设备对分组后沿连线法向等间距平移（间距 14px，最大偏移 18px），两端锚点只沿设备边框滑动、不会脱离设备；只剩一条时自动回到基准位置，不同设备对之间互不影响
- **端口连线跟随鼠标的虚线**：点击起点端口后出现实时跟随光标的虚线与端点圆点，点击另一设备端口即建立链路；按 `Esc` 或再次点击起点可取消
- **拓扑元素悬浮信息小窗**：鼠标移到设备 → 型号 / 类型 / 端口数（含 UP 数）/ 链路数 / VLAN 数 / 已配 IP；移到端口 → 状态 / 配置（Access/Trunk/三层 IP）/ 对端设备；移到链路 → 两端设备端口 + 两端状态。小窗 `pointer-events:none`，不拦截点击
- 坐标换算 `toLocal()` 增加兜底：拿不到 SVG `getScreenCTM()` 时改用画布矩形 + 视图变换换算
- 回归测试 `verify.js` 达 **286** 项断言全通过

## [1.6.1] — 2026-09-09 · 逐字输出逐行占位 + 长输出不再秒出
- 修复「先留出整块空白再逐行显示」：打字机改为**一行一行 `appendChild` 新节点**，屏幕空间随输出逐行增长（此前一次性建好所有空行节点，空节点同样占高度，所以看起来是"先把位置占满"）
- 修复 `display interface` 等长输出**仍然是秒出**：原先超过 240 行会直接放弃动画，现放宽到 3000 行，并改为按总时长上限自动提速 —— 实测 `dis int`（673 行 / 2 万字符）约 5 秒滚动完
- 命令回显行立即完整出现，之后输出行才开始逐字揭示，回车后的观感更接近真实设备
- `ping -t` 的新行排入当前动画队列尾部，不再打断正在刷的那一行
- 回归测试 `verify.js` 达 **250** 项断言全通过

## [1.6.0] — 2026-09-09 · PC 持续 ping（ping -t）+ 逐字输出节奏调优
- **PC / 服务器终端新增 `ping -t`**：持续探测目标，每 1 秒输出一行，`Ctrl+C` 中断并给出收发统计（已发送 / 已接收 / 丢失率 / 往返 min-avg-max）
- **持续 ping 实时响应拓扑变化**：每次探测都重新走转发路径计算 —— 链路断开或交换机端口 `shutdown` 立即变为 `Request time out`，恢复后自动回到 `Reply`（适合课堂上"一边 ping 一边改配置"的演示）
- 修复端口 down 时提示 `Ping: The source address does not exist.` 的误导：仍取网卡已配置的 IP，正确显示为 `Request time out`
- PC `ping` 同时支持 `-n <次数>`；`ping -t` 运行期间输入框只读，只能 `Ctrl+C` 中断（贴近真实终端行为）
- **逐字输出节奏调慢**：380 字符/秒、行间停顿 30ms、单次总时长上限 5 秒（原 900/12ms/2.6s），回显更接近真实设备
- 排查并确认所有有输出的路径（命令执行 / Tab 候选 / `?` 帮助 / `printOut` / 首屏横幅 / 持续任务）均已接入逐字揭示
- 新增 `Sim.pingOnce` / `Sim.pingStats`，终端新增 `startJob` / `stopJob` 后台任务调度
- 回归测试 `verify.js` 达 **247** 项断言全通过

## [1.5.0] — 2026-09-09 · 终端逐字逐行输出（贴近真实设备回显）
- 终端屏幕改为「一行一节点」渲染：命令回显与输出按 `\n` 拆成 `.t-line`，为逐字揭示提供基础
- 新增打字机效果：执行 `display` / `ping` 等命令后，输出像真实设备一样**逐字逐行刷出**，行末带轻微停顿
- 输出过程中末行显示闪烁方块光标；**按任意键 / 回车 / 空格 / 点击屏幕**可立即跳到完整内容（回车只快进，不会误提交命令）
- 单次输出总时长上限约 2.6 秒，长输出自动提速；超过 240 行（如 `display current-configuration`）自动关闭动画避免卡顿
- 动画只作用于显示层：命令结果仍**立即完整写入会话 buffer**，历史回放与自动化测试不受影响
- 新增 `H.UI.Terminal.setTyping()` 开关，自动化回归可关闭动画
- 回归测试 `verify.js` 达 **226** 项断言全通过

## [1.4.1] — 2026-09-03 · ping/tracert 命令去歧义 + 关于面板收尾
- 修复 `ping <ip>` 与 `ping <word>` 在系统视图同时匹配导致的 `Error: Ambiguous command`（输入 IPv4 地址时报错）；`tracert` 同步修复
- engine 新增 `<host>` token（接受 IPv4 或主机名格式，字母开头 + 字母数字/连字符/点），统一覆盖「ping 主机名」与「ping IP」两种场景
- `Sim.ping` / `Sim.tracert` 已支持按设备主机名解析，无需两条命令并行
- 关于面板移除「当前版本」行后清理两个死变量（`ver` / `updated`）
- 新增 `.gitattributes`（`* text=auto eol=lf`），全仓统一 LF，根治 Contents API 同步把 CRLF 推上去的问题
- 回归测试 `verify.js` 达 **210** 项断言全通过

## [1.4.0] — 2026-09-03 · 关于面板 + 迭代版本管理 + GitHub 发布
- 新增「关于」按钮（位于帮助按钮旁）：展示作者、联系方式、GitHub 仓库地址、当前版本号与更新日志
- 建立迭代版本号管理机制（自 1.0.0 起，按日汇总版本跨度，回滚修改不记录）
- 新增 `version.json` / `js/core/version.js`（运行时版本数据源）/ `CHANGELOG.md`（GitHub 展示）
- 初始化 Git 仓库并发布至 GitHub（reginofchaos/h3c-sim），建立 Release 与版本标签
- 约定：以后每次更新自动更新版本号与更新记录，并同步 GitHub

## [1.3.0] — 2026-09-03 · 自查修复：tracert 逐跳显示
- 修复 `tracert` / `tracert6` 只显示目的地、不逐跳展开的问题（现正确逐跳展开中间路由器）
- IPv6 双栈模块全覆盖自检，无新增缺陷
- 回归测试 `verify.js` 达 **180** 项断言全通过

## [1.2.0] — 2026-09-02 · IS-IS 路由进程 + Route-Policy
- IS-IS 路由进程（is-level / network-entity / import-route），接口 isis enable / cost / circuit-level / circuit-type
- Route-Policy 多节点（if-match acl|ip-prefix、apply cost|preference|tag|next-hop）真实过滤与改写路由属性
- 新增场景 6：IS-IS + Route-Policy 实验
- 修复 display isis peer 主机名显示、undo route-policy 节点删除失败
- 回归测试 `verify.js` 达 **142** 项断言全通过

## [1.1.0] — 2026-09-01 · 广域网 / NAT / VRRP / IPv6 四大模块 + 体验修复
- 广域网接入模块(WAN)：Serial 口 PPP/HDLC 封装、PAP/CHAP 认证与状态机
- NAT 地址转换：地址池 / NAPT / 静态 NAT / 内部服务器(DNAT) + 真实转发与会话表
- VRRP 网关冗余：主备选举、虚拟网关、故障切换与抢占
- IPv6 双栈端到端转发：ipv6 地址 / 静态路由、forwardPath6、真实 ping6 / tracert6、display ipv6 *
- 拓扑 UI 优化：设备动态宽度、链路标签浮层、面板重显按钮、右侧设备详情
- 保存/导出三重兜底（File System Access API / msSaveOrOpenBlob / 手动复制弹窗）
- 4 个场景增加「实验目标 / 步骤 / 预期」
- CLI 修复：help 列表显示完整子句、dis cu 展示已连默认端口、接口名四种写法统一解析
- 修复 IF_ABBR 与 Serial 端口名冲突、undo route-policy 死命令等
- 回归测试 `verify.js` 由 73 项提升至 **172** 项全通过

## [1.0.0] — 2026-08-31 · 项目立项 / 核心首发
- 纯前端 H3C Comware 风格交换机 / 路由器仿真终端（无框架、无构建步骤、原生 JS）
- 核心转发引擎：L2/L3 转发、STP、ping / tracert 全网仿真
- CLI 引擎：视图体系、? 帮助、Tab 补全、历史、undo
- 命令模块：基础管理 / 二层(VLAN/STP/聚合/镜像) / 三层 / 静态·RIP·OSPF·BGP 路由 / ACL / QoS / 安全(AAA/SSH/Telnet/802.1X) / 网络管理(SNMP/NTP/日志) / 监控(display 系列)
- 拓扑编辑器：拖拽添加、端口到端口连线、链路状态指示、设备删除 / 重置
- 工程存取：保存/读取(localStorage)、保存/打开工程文件(.json)、导出/导入 .cfg 配置
- PC / Server 主机 CLI（ipconfig / ping / tracert / set ip 等）
- 4 个实验教学场景
- 回归测试脚本 `verify.js`（73 项断言）
- 修复 monitor.js 语法错误、state.js 导出漏字段、sim.js 数据指向错误、loadScenario 连线索引错位、sysname 视图受限、新建设备默认 hostname 等

> 注：2026-08-31 当日曾对拓扑 UI 做一轮优化后又整体回滚，按「回滚的修改不记录」约定未列入本日志。
